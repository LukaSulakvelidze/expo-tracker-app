import { StyleSheet, Text } from "react-native";

export default function TrackCreateScreen() {
  return <Text style={styles.text}>TrackCreateScreen</Text>;
}

const styles = StyleSheet.create({
  text: {
    fontSize: 40,
  },
});
