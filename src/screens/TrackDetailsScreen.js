import { StyleSheet, Text } from "react-native";

export default function TrackDetailsScreen() {
  return <Text style={styles.text}>TrackDetailsScreen</Text>;
}

const styles = StyleSheet.create({
  text: {
    fontSize: 40,
  },
});
